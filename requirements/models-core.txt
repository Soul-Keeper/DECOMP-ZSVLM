# ViLT, BLIP, CLIP, Florence-2, PaliGemma, Qwen2.5-VL, and the Grounding DINO reference. 
# Python 3.11.15
#
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements/models-core.txt
#   pip install https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.9.47/flash_attn-2.7.4+cu130torch2.13-cp311-cp311-linux_x86_64.whl
#
# flash-attn is optional; the wrappers fall back to eager attention when it is
# absent, which changes latency but not the outputs. Prebuilt wheels for other
# CUDA and torch combinations: https://mjunya.com/flash-attention-prebuild-wheels/

transformers==4.49.0
tokenizers==0.21.4
accelerate==1.14.0
huggingface-hub==0.36.2
safetensors==0.8.0
qwen-vl-utils==0.0.14
timm==1.0.28
sentencepiece==0.2.2
protobuf==7.35.1
einops==0.8.2
numpy==2.4.4
pillow==12.2.0
